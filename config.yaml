llm:
  model: "gpt-4o-mini"
  temperature: 0.1
  max_tokens: 1000

retrieval:
  embedding_model: "all-MiniLM-L6-v2"
  top_k: 3
  index_path: "data/faiss_index"

tools:
  enabled:
    - spec_search
    - log_classifier
    - code_explainer

api:
  host: "0.0.0.0"
  port: 8000